# VazinCMS 10.30.2

## نسخه نگهداری و آمادگی تولید

- تکمیل قراردادهای بازپرداخت Travel/Visa و idempotency
- اصلاح VazinPay و Update Feed
- افزودن مستندات عمومی و مسیرهای OpenAPI
- سخت‌سازی نصب، rollback و artifact انتشار

# VazinCMS 10.30.1

## اصلاح قرارداد Webhook وزین‌پی

- اعتبارسنجی Webhook در Travel/Visa با قرارداد عمومی VazinPay همسان شد: کلید امضا از SHA-256 راز Webhook مشتق می‌شود.
- فروش تجاری Travel/Visa همچنان غیرفعال و جدا از محیط Sandbox باقی می‌ماند.

# VazinCMS 10.30.0

## فاز ۱۶ — اتصال Sandbox عمومی Travel/Visa

- اتصال ماژول عمومی Travel/Visa به Public Project رسمی VazinPay روی `api.pay.vazin.online` تکمیل شد.
- callback آزمایشی می‌تواند با allow-list و امضای معتبر فعال باشد، درحالی‌که فروش تجاری با `TRAVEL_VISA_COMMERCIAL_ENABLED=false` بسته می‌ماند.
- قرارداد امضای webhook با مشتق SHA-256 secret رسمی VazinPay همسان شد و تست‌های create/readback/webhook به release gate افزوده شدند.
- هیچ فروش واقعی، PII production یا پردازش واقعی در این انتشار فعال نمی‌شود.

# VazinCMS 10.10.10

## یکپارچه‌سازی معرفی عمومی Vazin Travel

- منوی پوستهٔ فعال Travel فقط مسیرهای محصول، امکانات، مدل فروش، دمو و لایسنس را نشان می‌دهد؛ صفحات مصرف‌کنندهٔ قدیمی حذف نشده‌اند و صرفاً Demo Template هستند.
- metadata صفحهٔ اصلی در fa/ru/en/ar صریحاً White-label / Licensing است و صفحات demo با `noindex,follow` برچسب می‌خورند.
- فروش مستقیم، provider، checkout، پرداخت و delivery خارجی بدون تغییر و fail-closed باقی می‌مانند.
- این انتشار فقط از نسخهٔ 10.10.9 ارتقا می‌یابد.

# VazinCMS 10.10.9

- معرفی و ناوبری Travel برای فروش B2B وایت‌لیبل و لایسنس یکدست شد؛ هیچ فروش مستقیم سفر توسط Vazin معرفی یا فعال نمی‌شود.
- صفحات قدیمی سفر حذف نمی‌شوند و در tenant رسمی به‌عنوان Demo Template برای شخصی‌سازی مشتری لایسنس نمایش داده می‌شوند.
- preflight محلی و fail-closed برای manifest دمو tenant وایت‌لیبل حفظ شد؛ هیچ tenant، provider، پرداخت یا اتصال خارجی ایجاد نمی‌کند.
- این انتشار فقط از 10.10.8 ارتقا می‌یابد.

# VazinCMS 10.10.8

- پنل مدیریت در نبود override محیط، زبان پیش‌فرض برند را از `cms_settings`
  می‌خواند؛ انتخاب صریح کاربر، ترجیح Vazin ID و `DEFAULT_LOCALE` همچنان مقدم‌اند.
- ناوبری پنل در عرض‌های دسکتاپ اکنون برای ماژول‌های بیشتر wrap می‌شود و موجب
  اسکرول افقی صفحه نمی‌شود.
- این انتشار فقط از 10.10.7 ارتقا می‌یابد و رفتار فروش، پرداخت، رزرو، provider
  و delivery خارجی را تغییر نمی‌دهد.

# VazinCMS 10.10.7

- رنگ متن فوتر رابط عمومی Travel و Visa از token استاندارد پلتفرم می‌آید تا
  روی پس‌زمینهٔ واقعی هر پوسته کنتراست WCAG AA داشته باشد.
- این انتشار فقط از 10.10.6 ارتقا می‌یابد و رفتار فروش، پرداخت، رزرو، provider
  و delivery خارجی را تغییر نمی‌دهد.

# VazinCMS 10.10.5

نسخهٔ 10.10.5 hotfix کنترل‌شده برای فرم همکاری آژانس در Travel است: متن اختصاصی
این مسیر از متغیرهای chrome پوسته جدا شده تا رندر مشترک پوسته، کلیدهای فرم را
بازنویسی نکند. مسیرهای agency در fa، ru و en و حالت ثبت‌شدهٔ فرم تحت قرارداد
تست رندر قرار دارند.

# VazinCMS 10.10.4

نسخهٔ 10.10.4 hotfix کنترل‌شده برای مسیر پروندهٔ دستی eVisa است: متن اختصاصی
فرم از متغیرهای پوسته جدا شده و صفحهٔ fallback همان CSS پوستهٔ فعال و برچسب‌های
B2B را می‌گیرد. مرزهای fail-closed فروش مستقیم، پرداخت و delivery تغییری نکرده‌اند.

# VazinCMS 10.10.3

نسخهٔ 10.10.3 ارتقای کنترل‌شده از 10.10.2 است. مسیر عمومی Visa اکنون فقط
پروندهٔ دستی eVisa می‌سازد: بدون قیمت، checkout، فاکتور یا پرداخت آنلاین. فرم
کامل متقاضی رمزنگاری‌شده باقی می‌ماند و فقط owner/admin آن را با پاسخ no-store
می‌بیند. فروش مستقیم و callback پرداخت با feature gate پیش‌فرض خاموش‌اند و
Telegram نیز همچنان کاملاً رضایت‌محور و بدون delivery پیش‌فرض است.

# VazinCMS 10.10.2

- پوستهٔ فعال پیش‌فرض اکنون fontِ Vazirmatn را preload و stylesheet پلتفرم را بعد از assets تم بارگذاری می‌کند؛ shell، کارت‌ها، فاصله‌ها و کنترل‌ها در Travel و Visa یکسان و responsive می‌مانند.
- کنترل‌های فرم و selectorها از font مشترک ارث می‌برند و focus-visible برای کیبورد قابل مشاهده است.
- این انتشار فقط از 10.10.1 ارتقا می‌یابد؛ delivery تلگرام و مسیرهای بیرونی بدون feature gate و رضایت صریح همچنان غیرفعال‌اند.

# VazinCMS 10.10.1

- مسیر انتشار استاندارد برای ارتقای مستقیم tenantهای Travel و Visa از 10.6.1
  به 10.10.1 با runtime خارجی و journal اتمی افزوده شد.
- rollback خودکار فقط code را بازمی‌گرداند و runtime/SQLite خارجی را برای
  جلوگیری از حذف داده‌های جدید دست‌نخورده نگه می‌دارد.

## 10.10.0

- پلتفرم وایت‌لیبل Travel برای آژانس‌های دارای مجوز: دریافت lead، branding
  مستقل tenant و connectorهای قراردادی با نگه‌داری رمزنگاری‌شدهٔ credential.
- فرم کامل، پرونده‌محور و رمزنگاری‌شدهٔ eVisa؛ ثبت فرم هرگز به معنای صدور یا
  فروش خودکار ویزا نیست.
- هشدارهای Telegram برای مدیر و مشتری فقط پس از opt-in صریح، با صف رمزنگاری‌شده،
  `/stop` و سه feature gate پیش‌فرض خاموش.
- migrationهای SQLite و PostgreSQL، قراردادهای آزمون و QA رابط RTL برای این
  قابلیت‌ها افزوده شد.

## 10.9.2

- انتشار bootstrap-only برای سایت managed با قرارداد صریح `managed-site-v2`.
- provision تازهٔ SQLite، logs و directoryهای runtime محدود؛ ایجاد مالک، migration و
  بررسی Connector v2 به worker سازگار VazinOnline 16.8.24 واگذار می‌شود.
- entrypoint جدا از installer و rollback ارتقاهای legacy تا مسیر fresh با مسیر
  release قبلی اشتباه نشود.

## 10.9.1

- مدیریت برنامهٔ هفتگی و تولید کنترل‌شدهٔ زمان‌های رزرو از پنل دستیار.
- رد مستقل درخواست رزرو و اعلان داخلی idempotent برای مدیر سایت.
- پاسخ `503` واقعی از health هنگام قطع پایگاه داده و بودجهٔ امن worker.
- جلوگیری از هم‌پوشانی زمان‌ها با احتساب buffer و عبور امن Worker از ردیف‌های نامعتبر صف.

## 10.9.0

- دستیار Telegram Business با دریافت `business_connection` و پیام مشتری، بدون نشست شخصی یا MTProto.
- Mini App روسی/انگلیسی برای معرفی خدمات، مشاهدهٔ زمان آزاد و رزرو idempotent.
- تحویل گفتگو به اوکسانا یا مدیر سایت با توقف فوری پاسخ خودکار همان مکالمه.
- صف مستقل دستیار، محدودیت نرخ، نگهداری حداقلی داده و قفل‌های تحویل پیش‌فرض خاموش.
- حالت preview و fixture آفلاین برای آزمون کامل پیش از اتصال واقعی.

## 10.8.0

- ماژول Telegram برای Webhook، همگام‌سازی دوطرفه، آرشیو تاریخ‌دار، واردکردن کل تاریخچه و Web App مدیر.
- تنظیم مستقیم و ماژولار Vazin ID برای سایت‌های مشتری، با PKCE و provision قابل‌کنترل.
- موتور SEO، ویرایش کامل metadata، `sitemap.xml`، `robots.txt`، اخطارهای غیرمسدودکننده و برنامهٔ پیشنهادی انتشار.
- Hookهای ماژولار برای رویداد محتوا و پردازش زمان‌بندی‌شده.

## 10.7.0

- هستهٔ بسته‌های مستقل برای قالب و ماژول، نصب امن ZIP و نگهداری افزونه‌های مشتری در runtime پایدار.
- Router ماژولار و انتقال مالکیت مسیرهای سفر، ویزا، انتشار و VazinPay به بسته‌های همراه.
- قالب پایهٔ مستقل، template hierarchy و مدیریت یکپارچهٔ نصب/فعال‌سازی/بایگانی از پنل مالک.
- حذف `itgala.ru` از allowlist انتشار VazinCMS بدون تغییر در سایت زنده.

## 10.6.2

- افزودن watermark اتمیک `profile_revision` برای جلوگیری از بازگشت نام یا تنظیمات قدیمی پس از لغو اشتراک.
- قفل ردیف PostgreSQL و `BEGIN IMMEDIATE` در SQLite برای ترتیب قطعی callbackهای هم‌زمان.
- مهاجرت idempotent 10.6.2 و نصب/rollback محدود به predecessor دقیق 10.6.1.

## 10.6.1

- اتصال Vazin ID فقط با `sub` پایدار؛ provision جدید تنها با `email_verified=true` و collision نیازمند اتصال صریح کاربر محلی است.
- همگام‌سازی نام استاندارد مستقل از preferences و projection اختیاری نام کاربری، تصویر، زبان، قالب و updated_at با provenance دقیق.
- قرارداد `preferences_shared`: مقدار غایب حفظ، false پاک‌سازی فقط دادهٔ منبع Vazin ID و true snapshot قطعی؛ override محلی حفظ می‌شود.
- پشتیبانی واقعی رابط fa/en/ru/ar بدون ترجمه یا تغییر داده‌های پویا و کنترل سخت‌گیرانهٔ URL تصویر HTTPS.
- runtime خارجی و پایدار برای storage/uploads، نصب اتمیک fail-closed، journal بازیابی durable و rollback کد با حفظ داده برای چهار سایت allowlist‌شده.

# VazinCMS 10.1.0

- ورود مرکزی با Vazin ID 2.6.0، PKCE و Consent
- پیوند امن حساب‌های قدیمی و صفحه حساب مشترک
- ورود محلی اضطراری فقط برای Owner

# VazinCMS 10.0.1

- تفکیک سرویس‌های Vazin ID، VazinPay و Vazin Online با مهاجرت امن تنظیمات نسخه 10.0.0
- سازگاری رسمی با Vazin Online 9.1.2 و Connector v2
- اصلاح namespace، Session و مستندات قدیمی Vazin Online در هسته CMS

# VazinCMS 8.0.2

- انتشار اصلاحی با شناسه نسخه یکتا برای مخزن بسته‌های VazinOnline
- هماهنگی نسخه در Manifest، فایل VERSION و بسته نصب
- حفظ کامل تنظیمات، داده‌ها و مسیر بازگشت هنگام ارتقا

# VazinCMS 5.2.2

- اصلاح کوئری محدودسازی ورود در SQLite با انتخاب گویش SQL پیش از `prepare`
- تفکیک خطای نشست/CSRF منقضی از خطای سرویس دیتابیس
- حفظ سازگاری کامل مسیر ورود با PostgreSQL و SQLite

# VazinCMS 5.1.2

- رفع خطای داخلی صفحه ورود در حضور نشست قدیمی یا ناقص
- ترمیم جداول نشست فعال و محدودسازی تلاش ورود هنگام ارتقا
- حفظ حساب مدیر، محتوا، تنظیمات، قالب و ماژول‌ها

- رفع اجرای اشتباه schema و migrationهای SQLite روی PostgreSQL در نصب خودکار
- تشخیص صریح درایور دیتابیس و انتخاب فایل‌های `pgsql` یا `sqlite`
- ثبت نسخه اولیه با دستور سازگار با هر پایگاه داده
- توقف ایمن روی درایور ناشناخته پیش از هرگونه تغییر دیتابیس

# VazinCMS 5.0.0

- Manifest نسل دوم سازگار با مرکز انتشار Vazin Online 5.7
- اعلام حداقل نسخه CMS و پنل مرکزی برای پیش‌بررسی مقصد
- یادداشت انتشار و سطح ریسک قابل نمایش پیش از نصب
- آماده‌سازی بسته برای کنترل ساختار، checksum و downgrade امن

# VazinCMS 4.4.0

- Endpoint امضاشده اطلاعات استقرار برای پیش‌نمایش پنل مرکزی
- گزارش نسخه هسته، PHP، نوع دیتابیس و ماژول‌های فعال
- اعلام رسمی قابلیت پشتیبان ثبت‌شده و Rollback
- Manifest سازگار با انتخاب مقصد Vazin Online 5.5.0

# Vazin Online 5.0.0

- فرم عمومی درخواست سفر، ویزا، هتل، پرواز و مشاوره
- شماره سفارش و کد پیگیری محرمانه برای حساب مشتری
- نمایش وضعیت سفارش، مبلغ و وضعیت پرداخت
- اتصال مستقل VazinPay با callback امضاشده و ثبت idempotent پرداخت
- تثبیت رسمی هات‌فیکس موتور View نسخه 4

## نسخه‌های پیشین

# Vazin Online 4.0.0

- راه‌اندازی نمای عمومی واقعی travel.vazin.online
- صفحه اصلی، مقصدها، خدمات، مقاله‌ها و صفحات عمومی در ۹ زبان
- مسیر مستقل و قابل اشتراک برای هر محتوای منتشرشده
- متادیتای SEO، داده ساختاریافته، Sitemap، Robots و RSS
- طراحی سریع و واکنش‌گرا برای موبایل و دسکتاپ
- نمایش مستقیم محتوای منتشرشده پنل مرکزی

# Vazin Online 3.0.0

- ادغام مدیریت محتوای سفر بر پایه VazinCMS 6.0.2
- مدیریت صفحه، مقصد، خدمت سفر و مقاله از پنل مرکزی
- پشتیبانی از ۹ زبان، SEO، تصویر شاخص و چرخه انتشار
- اتصال travel.vazin.online به رجیستری مرکزی
- مهاجرت امن PostgreSQL و SQLite

# Vazin Online 1.6.0

- جست‌وجوی مستقیم مشتری با شناسه عددی، نام، ایمیل، تلفن یا شرکت
- فیلتر وضعیت و نقش همراه پاک‌سازی سریع فیلترها
- نمایش شناسه و اطلاعات تماس در جدول مشتریان
- صفحه ۳۶۰ درجه مشتری شامل کیف پول، سفارش، فاکتور، سرویس و تیکت
- کارت‌های خلاصه فعالیت و لینک مستقیم به سوابق مرتبط
- طراحی واکنش‌گرا برای مدیریت مشتریان در موبایل
- بدون تغییر ساختار پایگاه‌داده

# Vazin Online 1.5.0

- مدیریت اعضای تیم از صفحه مستقل مالک
- مجوزهای جزئی برای مشتریان، فروش، سرویس‌ها، پشتیبانی، مالی و سامانه
- نمایش منوها براساس مجوز واقعی هر مدیر یا پشتیبان
- ثبت نشست‌های ورود همراه IP، دستگاه و آخرین فعالیت
- خروج اجباری عضو تیم از همه دستگاه‌ها و لغو فوری نشست‌ها
- فعال یا تعلیق‌کردن حساب مدیر و پشتیبان
- اصلاح گزارش نسخه در Health Check، وضعیت سامانه و گزارش خطا
- کنترل انتشار برای جلوگیری از ناپدیدشدن دوباره منو

# Vazin Online 1.4.1

- اصلاح فوری ناپدیدشدن منو پس از ارتقا به 1.4.0
- بازیابی صحیح کاربر واردشده و نقش او پیش از ساخت ناوبری
- افزودن کنترل انتشار برای جلوگیری از تکرار منوی نامرئی
- حفظ کامل گروه‌بندی، واکنش‌گرایی و سطح دسترسی منوی 1.4.0

# Vazin Online 1.4.0

- بازطراحی منوی بالای پنل با حفظ دسترسی سریع به بخش‌های اصلی
- گروه‌بندی بخش‌های مالی و مدیریتی در منوهای بازشونده
- نمایش واضح صفحه فعال و گروه مرتبط
- منوی واکنش‌گرا برای موبایل، تبلت و نمایشگرهای باریک
- رعایت سطح دسترسی مالک، مدیر و کاربر در گزینه‌های ناوبری
- بهبود دسترسی‌پذیری با کنترل صفحه‌کلید و ویژگی‌های ARIA

# Vazin Online 1.3.0

- مدیریت وظایف زمان‌بندی‌شده از پنل مالک
- اجرای خودکار صف Webhook، اعلان‌ها و کنترل انقضای سرویس‌ها
- پاک‌سازی قابل‌تنظیم رویدادهای قدیمی
- قفل ۱۵ دقیقه‌ای برای جلوگیری از اجرای هم‌زمان
- اجرای دستی، فعال/غیرفعال‌سازی و تاریخچه نتیجه و مدت اجرا
- سرویس و Timer مستقل systemd با اجرای هر دقیقه

# Vazin Online 1.2.0

- مقصدهای Webhook با انتخاب رویداد برای هر اتصال
- امضای HMAC-SHA256 و رمزگذاری راز امضا
- صف پایدار، تلاش مجدد زمان‌بندی‌شده، تاریخچه تحویل و رویداد آزمایشی
- جلوگیری از مقصدهای خصوصی و محلی برای کاهش خطر SSRF

# Vazin Online 1.1.0

- API خواندنی نسخه‌بندی‌شده برای مشتریان، فاکتورها، سرویس‌ها و کیف پول‌ها
- احراز هویت Bearer با کلید هش‌شده، scope محدود، انقضا و لغو کلید
- ثبت آخرین استفاده و گزارش مسیر، وضعیت و زمان پاسخ درخواست‌های API
- پنل اتصال‌ها با تاریخ انقضا و گزارش ۱۰۰ درخواست آخر

# Vazin Online 1.0.0

- نخستین انتشار پایدار هسته Vazin Online
- مدیریت کلیدهای اتصال برای ربات‌ها و محصولات، با نمایش یک‌باره رمز
- ذخیره امن هش کلید، دامنه دسترسی و قابلیت لغو فوری
- انقضای خودکار نشست‌های غیرفعال و طولانی
- هدرهای CSP و Permissions Policy برای کاهش سطح حمله مرورگر
- مهاجرت امن از 0.9.0 بدون تغییر اطلاعات موجود

# Vazin Online 0.5.0

- کیف پول چندارزی مشتریان و دفتر گردش تغییرناپذیر
- افزایش و کاهش کنترل‌شده موجودی با ثبت مانده پس از تراکنش
- پرداخت اتمیک فاکتور از کیف پول و همگام‌سازی سفارش
- فیلتر کیف پول‌ها، گزارش مانده و ثبت رویدادهای مالی

# Vazin Online 0.4.0

- صدور فاکتور یکتا از روی سفارش
- ثبت پرداخت قطعی با روش و شناسه تراکنش
- محاسبه خودکار وصول‌شده و مانده بدهی
- پشتیبانی پرداخت ناقص و تسویه کامل
- همگام‌سازی وضعیت فاکتور و سفارش پس از تسویه
- گزارش مالی تفکیک‌شده بر اساس ارز
- مهاجرت امن از 0.3.0 بدون حذف اطلاعات

## 0.3.0

- مدیریت محصولات، قیمت، ارز و دوره صورتحساب
- ثبت و پیگیری سفارش مشتریان با شماره یکتا
- ساخت خودکار سرویس پس از تکمیل سفارش
- مدیریت وضعیت، شناسه و انقضای سرویس‌ها
- نمایش سرویس‌ها در پروفایل مشتری
- داشبورد آماری سفارش و سرویس
- مهاجرت امن از 0.2.0 و حفظ storage و حساب مالک

## 0.2.0

- مدیریت مشتریان، جست‌وجو، فیلتر، صفحه‌بندی و پروفایل
- فعال‌سازی، تعلیق، انتظار و بستن حساب
- تیکت، اولویت، وضعیت، پاسخ و یادداشت داخلی
- داشبورد آماری و ثبت ممیزی تغییرات
- مهاجرت امن از 0.1.0
- اصلاح دسترسی `.env` برای PHP-FPM
# 0.6.0
- Added service renewal settings and due-date queue.
- Added idempotent renewal invoice generation.
- Added PostgreSQL and SQLite migration coverage for renewals.
# 0.8.0

- مرکز اعلان‌ها و یادآوری‌های عملیاتی
- تولید اعلان سررسید فاکتور و انقضای سرویس
- جلوگیری از اعلان تکراری با کلید یکتا
- صف قابل‌فیلتر و ثبت وضعیت ارسال یا لغو

## 0.8.0
- Added discount and coupon management.
- Added atomic coupon redemption with usage limits and validity windows.
- Added order subtotal, discount amount, final total, and redemption audit trail.
# 0.9.0

- مدیریت نرخ‌های مالیات بر اساس کشور و ارز
- ثبت مبلغ پایه، نرخ و مبلغ مالیات به‌صورت ثابت روی فاکتور
- محاسبه یکسان مالیات برای فاکتور سفارش و تمدید سرویس
- نمایش شفاف اجزای مبلغ نهایی فاکتور
## 1.5.1

- خواندن شماره نسخه از فایل واحد `VERSION` در داشبورد، وضعیت سامانه، API، Health و پایین صفحات
- حذف شماره‌های ثابت قدیمی از رابط کاربری و گزارش خطا
- افزودن کنترل انتشار برای جلوگیری از باقی‌ماندن شماره نسخه قدیمی
# 2.0.0

- مرکز ثبت و پایش ماژول‌های VazinCMS، VazinPay، ربات و سرویس
- API نسخه ۲ با گزارش قابلیت‌های هسته و فهرست ماژول‌ها
- Heartbeat امن برای ثبت نسخه، سلامت و آخرین ارتباط هر ماژول
- دامنه‌های دسترسی مستقل برای محتوا، مشتری، سفارش و ماژول
- ثبت اولیه VazinCMS Travel و نقشه آماده ادغام نسخه ۳
- مهاجرت سازگار با PostgreSQL و SQLite

# 1.7.0

- تغییر مستقیم موجودی کیف پول از صفحه یکپارچه مشتری
- الزام ثبت دلیل برای هر افزایش یا کاهش موجودی
- جلوگیری از ثبت دوباره ناخواسته یک درخواست مالی
- نمایش آخرین گردش تمام کیف پول‌های مشتری در یک جدول
- ثبت مانده قبل و بعد، مدیر انجام‌دهنده و دلیل در گزارش حسابرسی
# 4.0.1
- اصلاح قرارداد HMAC مسیر `/connector/v1/health` با خط جدید واقعی و سازگار با Vazin Online 5.1.3
- افزودن کنترل انتشار برای جلوگیری از بازگشت خطای امضای Connector
- حفظ سازگاری نصب و ارتقا با PostgreSQL و SQLite بدون حذف اطلاعات یا تغییر کلید موجود
# 4.1.0
- هسته عمومی و خام برای هر سایت؛ بدون فعال‌سازی پیش‌فرض سفر
- مدیریت صفحه‌ها، صفحه اصلی چندزبانه و تنظیمات مستقل سایت
- مرکز ماژول‌های مستقل Travel و Visa با فعال‌سازی برای هر نصب
# 4.2.0
- ویرایش و حذف واقعی صفحه‌ها و مدیریت منوهای چندزبانه.
- مسیر عمومی مستقل برای ماژول‌های فعال سفر و ویزا.
- مهاجرت امن SQLite و PostgreSQL بدون حذف محتوای موجود.
# 4.3.0
- قرارداد استاندارد vazin-package.json برای مرکز به‌روزرسانی Vazin Online
- شناسایی مستقل محصول، نوع بسته، نسخه و ورودی نصب
- سازگاری با نصب و ارتقای صف‌بندی‌شده بدون حذف محتوا و تنظیمات

# 4.4.0
- گزارش امضاشده نسخه، PHP، پایگاه داده و ماژول‌های فعال به پنل مرکزی
- پشتیبانی از نصب هدفمند و Rollback ثبت‌شده برای هر سایت

# 4.5.0
- سازگاری کامل با Self Update و مرکز بسته‌های Vazin Online 5.6
- حفظ محتوا، تنظیمات، پایگاه داده و کلید Connector هنگام ارتقای بدون SSH
- ثبت نسخه واقعی CMS برای سایت‌های تازه و ارتقای مدیریت‌شده
# 5.1.0
- تبدیل هسته به CMS خام و حذف فعال‌سازی داخلی Travel و Visa.
- مدیریت صفحات، صفحه اصلی هر زبان، منوها، هویت، لوگو و اطلاعات تماس.
- مدیریت زبان‌های فعال و ورود مستقیم مدیر همان سایت.
- رجیستری عمومی برای نصب و فعال‌سازی ماژول‌های مستقل.
# 5.2.0

- Connector 2 با nonce یک‌بارمصرف و امضای HMAC کوتاه‌عمر
- ترمیم خودکار جداول نشست و محدودسازی ورود پیش از نمایش فرم login
- گزارش سلامت مدیر فعال، دیتابیس، صفحه ورود و نسخه Connector
- حفظ حساب‌ها، رمزها، محتوا، قالب‌ها و ماژول‌های موجود
# 7.0.0
- تم روشن، تاریک و هماهنگ با دستگاه برای سایت و پنل مدیریت
- مسیر انتخاب شرایط ویزا براساس ملیت و مقصد
- مسیر مقصدهای سفر متناسب با مخاطب فارسی و روسی
- زیرساخت کیت‌های شروع آماده فروش و قابل ویرایش
- حفظ انتخاب ظاهر در تمام صفحه‌ها
# 9.0.0

- سه پروفایل قالب شرکتی، سفر/ویزا و پرداخت
- تشخیص خودکار قالب مناسب از دامنه سایت هنگام ارتقا
- صفحه اصلی کامل و قابل تنظیم با Hero، آمار، مزایا، مراحل، میانبر خدمات، FAQ و CTA
- مدیریت رنگ، متن‌ها، دکمه‌ها، شبکه‌های اجتماعی و محتوای ساختاریافته از پنل
- اتصال دوباره مسیرهای عمومی مقصد، خدمات، مقاله، درخواست و پیگیری سفارش
- مسیرهای sitemap.xml، robots.txt و feed.xml
- حالت روشن/تاریک، منوی موبایل و طراحی واکنش‌گرا
- متادیتای Open Graph و داده ساختاریافته Organization
- حفظ داده‌ها و سازگاری ارتقا از VazinCMS 4.5.0 به بعد
