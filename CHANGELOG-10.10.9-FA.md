# VazinCMS 10.10.9

## وایت‌لیبل Travel و پیش‌پرواز دمو

- صفحهٔ اصلی Travel و ناوبری عمومی صریحاً برای فروش لایسنس B2B وایت‌لیبل تنظیم شدند؛ Vazin فروشندهٔ مستقیم سفر، بلیت یا ویزا معرفی نمی‌شود.
- منوی عمومی روی «محصول، امکانات، مدل فروش، دموی سایت آژانس و دمو/لایسنس» متمرکز است.
- صفحات محتوایی قدیمی حذف نشده‌اند؛ در tenant رسمی Vazin به‌عنوان Demo Template برچسب می‌خورند تا نمونهٔ قابل شخصی‌سازی برای صاحب لایسنس باشند.
- هیچ قیمت لایسنس، provider، checkout، رزرو یا پرداختی با این تغییر فعال نمی‌شود.

## پیش‌پرواز دمو tenant وایت‌لیبل

- ابزار محلی `scripts/tenant-demo-preflight.php` یک manifest دمو را فقط
  اعتبارسنجی و یک plan قرائت‌پذیر تولید می‌کند. خروجی به‌صراحت اعلام می‌کند که
  provisioning انجام نشده است؛ این ابزار tenant، دامنه، vhost، دیتابیس، حساب
  مدیر یا اتصال خارجی ایجاد نمی‌کند.
- manifest هر credential، token، secret، password، API key و webhook را رد
  می‌کند. فقط metadata غیرمحرمانهٔ connector در حالت‌های
  `lead_only`، `affiliate_redirect` و `manual_fulfilment` مجاز است.
- فروش مستقیم ویزا، delivery خارجی و همهٔ قابلیت‌های Telegram در plan خروجی
  صریحاً `false` هستند. `live_booking` در این مسیر رد می‌شود.
- نمونهٔ غیرواقعی و امن در `docs/tenant-demo.example.json` وجود دارد. برای
  provisioning واقعی همچنان تأیید صریح مالک برای دامنه و هدف سرور لازم است.
- این انتشار فقط از نسخهٔ 10.10.8 ارتقا می‌یابد و همچنان فقط دو tenant
  مدیریت‌شدهٔ موجود را پشتیبانی می‌کند.