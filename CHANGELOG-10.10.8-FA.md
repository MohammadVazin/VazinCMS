# VazinCMS 10.10.8

## زبان پیش‌فرض برند در پنل مدیریت

- اگر `DEFAULT_LOCALE` در محیط تعریف نشده باشد، پنل مدیریت اکنون زبان پیش‌فرض
  ذخیره‌شدهٔ همان برند در `cms_settings` را اعمال می‌کند؛ بنابراین سایت‌هایی که
  زبان پیش‌فرض‌شان فارسی است، `lang="fa"` و `dir="rtl"` درست می‌گیرند.
- انتخاب صریح کاربر، ترجیح زبان دریافت‌شده از Vazin ID و override محیط همچنان
  بر تنظیم برند مقدم‌اند. در اولین نصب یا هنگام خطای پایگاه‌داده، مسیر امن
  fallback به زبان مرورگر و سپس انگلیسی حفظ می‌شود.
- ناوبری پنل مدیریت در عرض‌های دسکتاپ برای ماژول‌های بیشتر wrap می‌شود تا
  عرض سند از viewport بزرگ‌تر نشود؛ در نمایشگرهای کوچک، منوی بازشوندهٔ موجود
  همچنان استفاده می‌شود.
- جدول‌های عملیاتی پنل، از جمله آمادگی و صف هشدار Telegram، روی موبایل داخل
  ناحیهٔ اسکرول افقی قابل‌دسترسی قرار می‌گیرند و دیگر عرض کل صفحه را نمی‌شکنند.
- این انتشار هیچ فروش مستقیم، رزرو، checkout، پرداخت، اتصال provider یا ارسال
  Telegram را فعال نمی‌کند. تمام gateهای delivery خارجی همچنان fail-closed هستند.
- این انتشار فقط از نسخهٔ 10.10.7 ارتقا می‌یابد.
