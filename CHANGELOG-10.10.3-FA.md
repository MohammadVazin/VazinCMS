# VazinCMS 10.10.3

- مسیر عمومی Visa از فروش مستقیم به بازکردن «پروندهٔ دستی eVisa» تغییر کرد؛
  پرونده صفرقیمت است، فاکتور یا checkout ایجاد نمی‌کند و `payment_status` آن
  `not_required` است.
- هر مسیر قدیمی checkout به‌صورت پیش‌فرض با
  `VAZIN_VISA_DIRECT_SALES_ENABLED=false` بسته است؛ callback پرداخت نیز در
  همین وضعیت پاسخ `410` می‌دهد. فعال‌سازی فقط پس از قرارداد، هویت حقوقی،
  بررسی پرداخت و مجوز فروشندهٔ دارای مجوز ممکن است.
- فرم کامل eVisa همچنان رمزنگاری‌شده در runtime خصوصی می‌ماند و فقط owner/admin
  در صفحهٔ پرونده، با پاسخ no-store و audit access، آن را به‌شکل ساختارمند
  می‌بیند. دادهٔ گذرنامه در Telegram یا log منتقل نمی‌شود.
- هشدار Telegram همچنان با رضایت و همهٔ feature gateهای delivery خاموش است.
