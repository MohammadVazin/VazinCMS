# VazinCMS 10.7.0 — هستهٔ ماژولار

- قرارداد رسمی `vazin-extension.json` برای قالب و ماژول با نسخه، وابستگی و سازگاری صریح.
- رجیستری واقعی افزونه‌ها، کشف بسته‌های همراه و اختصاصی، فعال‌سازی، غیرفعال‌سازی و بایگانی بازیابی‌پذیر.
- نصب ZIP محدود به مالک سایت با کنترل traversal، symlink، تعداد فایل، حجم استخراج و نوع فایل.
- نگهداری افزونه‌های مشتری در `storage/extensions` تا ارتقای هسته آن‌ها را حذف نکند.
- Router مستقل ماژول‌ها؛ سفر، ویزا، انتشار چندکاناله و VazinPay از Router اصلی جدا شدند.
- قالب پایهٔ وزین به بستهٔ مستقل با template hierarchy و سرویس امن asset منتقل شد.
- نصب تازه همهٔ migrationها را به ترتیب نسخه اجرا می‌کند و دیگر به فهرست دستی قدیمی وابسته نیست.
- `itgala.ru` از allowlist نصب و rollback VazinCMS حذف شد؛ این تغییر هیچ مهاجرتی روی سایت زنده انجام نمی‌دهد.
- قراردادهای جدید SQLite/PHP برای کشف، route، قالب، چرخهٔ فعال‌سازی و بایگانی افزونه.
