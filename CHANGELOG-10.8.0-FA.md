# VazinCMS 10.8.0

## Telegram

- اتصال چند سایت/کانال با Bot Token رمزگذاری‌شده و Webhook دارای `secret_token`.
- دریافت idempotent پست و ویرایش کانال و نگاشت هر پیام به یک پست CMS.
- حفظ تاریخ اصلی Telegram حتی برای مطالب چند سال قبل.
- واردکردن همهٔ پیام‌های `result.json` یا خروجی استاندارد MTProto بدون محدودیت ۵۰ پست.
- بازهمگام‌سازی امن و به‌روزرسانی پست‌های قبلی با شناسهٔ ثابت پیام.
- صف انتشار و ویرایش سایت به Telegram، retry تصاعدی و جلوگیری از loop دوطرفه.
- Telegram Web App با اعتبارسنجی HMAC رسمی و نگاشت صریح مدیر CMS.
- آرشیو عمومی جداگانه در `/{locale}/channel`؛ بدون افزودن اجباری به صفحهٔ اصلی.

## هویت، سئو و مدیریت محتوا

- تنظیم Vazin ID در پنل مشتری و حفظ fallback تنظیمات محیطی نسخه‌های قبلی.
- provision خودکار قابل خاموش‌کردن و ورود اضطراری مالک مستقل از Vazin ID.
- تحلیل SEO، canonical، Open Graph، robots، JSON-LD و تاریخ مقاله.
- ویرایش نوع صفحه/نوشته، تاریخ اصلی، Open Graph، robots و Schema از پنل محتوا.
- تولید عمومی `sitemap.xml` و `robots.txt` توسط ماژول SEO.
- اخطار پزشکی، تبلیغاتی و کیفی فقط برای مدیر؛ بدون سانسور یا توقف انتشار.
- اعلان داخل پنل و صف Telegram مدیر برای اخطارهای جدید.
- پیشنهاد دورهٔ انتشار قابل تنظیم و وضعیت‌گذاری پیشنهادها.

## زیرساخت

- Hookهای امن ماژول برای `content.saved`، `content.imported` و `scheduler.tick`.
- مهاجرت SQLite و PostgreSQL برای پیام‌ها، صف، تاریخچه، SEO و اخطارها.
- مسیر ارتقای دقیق از 10.7.0 با rollback کد و حفظ runtime.
