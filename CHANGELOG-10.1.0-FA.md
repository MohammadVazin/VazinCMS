# تغییرات VazinCMS 10.1.0

- ورود و ثبت‌نام مرکزی با Vazin ID 2.6.0 و OAuth 2.0 Authorization Code + PKCE
- پیوند حساب محلی موجود بر اساس ایمیل تأییدشده و شناسه عمومی ثابت
- جلوگیری از ساخت حساب تکراری و حفظ نقش، سفارش، کیف پول، خدمات و صورتحساب‌ها
- ایجاد حساب محلی Client برای کاربران جدید Vazin ID
- صفحه چندزبانه «حساب من» در مسیر `/{locale}/my-account`
- ورود رمزی محلی فقط برای Owner و استفاده اضطراری
- ابطال و کنترل نشست محلی CMS مستقل از نشست سرویس هویت
- عدم پیاده‌سازی مستقیم Google، Telegram یا Providerهای دیگر در CMS

مسیر بازگشت Client این سایت باید دقیقاً برابر `${APP_URL}/auth/vazin-id/callback` در Vazin ID ثبت شود.
