(()=>{const header=document.querySelector('.admin-header'),button=document.querySelector('.menu-toggle'),nav=document.querySelector('#admin-navigation');if(!header||!button||!nav)return;const close=()=>{header.classList.remove('menu-open');button.setAttribute('aria-expanded','false')};button.addEventListener('click',()=>{const open=header.classList.toggle('menu-open');button.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',event=>{if(event.target.closest('a')&&innerWidth<=920)close()});addEventListener('resize',()=>{if(innerWidth>920)close()});document.addEventListener('click',event=>{if(innerWidth<=920&&!header.contains(event.target))close()})})();
