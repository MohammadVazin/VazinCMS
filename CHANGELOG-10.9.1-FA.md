# VazinCMS 10.9.1

## تکمیل مدیریت رزرو دستیار

- برنامهٔ هفتگی هر خدمت و تولید کنترل‌شدهٔ زمان‌های قابل رزرو از پنل مدیریت.
- رد مستقل درخواست در انتظار و آزادسازی اتمیک زمان مربوط به آن.
- اعلان داخلی و بدون اطلاعات حساس برای مدیر هنگام ثبت درخواست رزرو جدید.

## پایداری عملیاتی

- مسیر health هنگام قطع پایگاه داده با HTTP 503 و `Cache-Control: no-store` پاسخ می‌دهد.
- اندازهٔ batch صف دستیار با سقف ۱۰، داخل بودجهٔ پنج‌دقیقه‌ای worker نگه داشته می‌شود.
- Worker بدون claim اضافه از سر ردیف‌های منقضی/نامعتبر صف عبور می‌کند و هیچ کار معتبری پشت آن‌ها معطل نمی‌ماند.
- تولید زمان و درج دستی، هم‌پوشانی بازه‌ها و bufferهای خدمت را رد می‌کند.
- مرزهای عمومی Mini App و Webhook با محدودیت نرخ و اتصال محافظت می‌شوند.

همهٔ قفل‌های Telegram، Business، delivery و پاسخ خودکار در ارتقا خاموش باقی می‌مانند و این نسخه هیچ توکن یا اتصال واقعی را فعال نمی‌کند.
