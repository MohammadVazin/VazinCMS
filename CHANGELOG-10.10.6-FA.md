# VazinCMS 10.10.6

- صف خصوصی درخواست‌های همکاری آژانس برای Travel افزوده شد: فیلتر وضعیت، مشاهدهٔ
  جزئیات، تخصیص به owner/admin فعال و ثبت audit؛ از ستون‌های موجود استفاده می‌کند
  و هیچ tenant، فروش، رزرو یا اتصال تأمین‌کننده ایجاد نمی‌کند.
- پنل Travel و Visa نمای فقط‌خواندنی آمادگی Telegram را نشان می‌دهد: سه feature
  gate، آماده‌بودن اتصال ربات و نمونه‌های رداکت‌شدهٔ پیام. این نمایش نه پیام، نه
  اشتراک و نه پیوند رضایت تولید نمی‌کند.
- delivery خارجی، Telegram، فروش مستقیم Visa و callback پرداخت همچنان پیش‌فرض
  خاموش و fail-closed هستند. نسخه فقط از 10.10.5 ارتقا می‌یابد.
