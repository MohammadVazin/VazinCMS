ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS width INTEGER;
ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS height INTEGER;
ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS sha256 VARCHAR(64) NOT NULL DEFAULT '';
ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS storage_driver VARCHAR(32) NOT NULL DEFAULT 'local';
ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS storage_key TEXT NOT NULL DEFAULT '';
ALTER TABLE cms_media ADD COLUMN IF NOT EXISTS metadata_json TEXT NOT NULL DEFAULT '{}';
CREATE UNIQUE INDEX IF NOT EXISTS idx_cms_media_sha256 ON cms_media(sha256) WHERE sha256<>'';
CREATE TABLE IF NOT EXISTS cms_media_derivatives(id BIGSERIAL PRIMARY KEY,media_id BIGINT NOT NULL REFERENCES cms_media(id) ON DELETE CASCADE,variant VARCHAR(64) NOT NULL,stored_name TEXT NOT NULL,mime_type VARCHAR(100) NOT NULL,width INTEGER,height INTEGER,file_size BIGINT NOT NULL DEFAULT 0,sha256 VARCHAR(64) NOT NULL,created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,UNIQUE(media_id,variant));
CREATE TABLE IF NOT EXISTS cms_media_collections(id BIGSERIAL PRIMARY KEY,collection_key VARCHAR(120) NOT NULL UNIQUE,label VARCHAR(255) NOT NULL,created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS cms_media_collection_items(collection_id BIGINT NOT NULL REFERENCES cms_media_collections(id) ON DELETE CASCADE,media_id BIGINT NOT NULL REFERENCES cms_media(id) ON DELETE CASCADE,position INTEGER NOT NULL DEFAULT 0,PRIMARY KEY(collection_id,media_id));
INSERT INTO schema_migrations(version) VALUES('10.13.0') ON CONFLICT(version) DO NOTHING;