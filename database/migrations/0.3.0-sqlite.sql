ALTER TABLE products ADD COLUMN description TEXT;
ALTER TABLE products ADD COLUMN price_amount NUMERIC NOT NULL DEFAULT 0;
ALTER TABLE products ADD COLUMN currency TEXT NOT NULL DEFAULT 'RUB';
ALTER TABLE products ADD COLUMN billing_cycle TEXT NOT NULL DEFAULT 'monthly';
ALTER TABLE products ADD COLUMN is_active INTEGER NOT NULL DEFAULT 1;
ALTER TABLE products ADD COLUMN updated_at TEXT;
CREATE TABLE IF NOT EXISTS orders (id INTEGER PRIMARY KEY AUTOINCREMENT,order_number TEXT NOT NULL UNIQUE,user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,product_id INTEGER REFERENCES products(id) ON DELETE SET NULL,product_name TEXT NOT NULL,amount NUMERIC NOT NULL,currency TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',notes TEXT,created_by INTEGER REFERENCES users(id) ON DELETE SET NULL,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE INDEX IF NOT EXISTS idx_orders_user ON orders(user_id,created_at DESC);
CREATE INDEX IF NOT EXISTS idx_orders_status ON orders(status,created_at DESC);
CREATE TABLE IF NOT EXISTS services (id INTEGER PRIMARY KEY AUTOINCREMENT,user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,product_id INTEGER REFERENCES products(id) ON DELETE SET NULL,order_id INTEGER REFERENCES orders(id) ON DELETE SET NULL,name TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',identifier TEXT,started_at TEXT,expires_at TEXT,notes TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);
CREATE INDEX IF NOT EXISTS idx_services_user ON services(user_id,status);
CREATE INDEX IF NOT EXISTS idx_services_expiry ON services(expires_at);
INSERT OR IGNORE INTO schema_migrations(version) VALUES('0.3.0');
